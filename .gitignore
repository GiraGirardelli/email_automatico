# --- SEGURANÇA (CRÍTICO) ---
# Ignora qualquer chave de criptografia (secret.key, chave_mestra.key, etc)
*.key

# Ignora qualquer banco de dados (emails_auto.db, emails_central.db, etc)
*.db

# --- SIMULAÇÃO E SERVIDOR ---
# Ignora a pasta que criamos para simular o servidor
SERVER_FAKE/
SIMULACAO_SERVIDOR/

# --- LIXO DO PYTHON E BUILD ---
# Ignora arquivos compilados e pastas de build do PyInstaller
__pycache__/
*.pyc
build/
dist/
*.spec

# --- OUTROS ---
*.log
.vscode/